apiVersion: v1
kind: Service
metadata:
  name: linkstore-svc
  namespace: linkstore
spec:
  type: ClusterIP
  selector:
    app: linkstore
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
